einops==0.8.0
geoopt==0.5.1
matplotlib==3.9.2
numpy==2.1.2
pandas==2.2.3
psutil==7.2.2
python-docx==1.2.0
reformer-pytorch==1.4.4
scikit-learn==1.5.2
scipy==1.14.1
seaborn==0.13.2
# PyTorch 的 CUDA wheel 与本机显卡驱动/CUDA 运行环境强相关。
# 推荐先按 docs/环境配置与运行说明.md 为本机 CUDA/CPU 单独安装 torch；
# 这里仅给出项目已验证的主版本范围，避免把不同设备强制降级到某个 CUDA 构建。
torch>=2.4,<3.0
tensorboard==2.18.0
tqdm==4.66.5
